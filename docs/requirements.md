# 要件定義書 — カフェ向けモバイルオーダーアプリ

**バージョン**: 1.8  
**作成日**: 2026-06-30  
**最終更新**: 2026-07-31（[Issue #7](https://github.com/Masaharu1223/AWS_OrderSystem/issues/7)・[Issue #8](https://github.com/Masaharu1223/AWS_OrderSystem/issues/8) の決定を反映。ゾーン別受付順（`queueSeq`）の採番方式をSQS FIFOからDynamoDB原子カウンタによる`order-fn`内同期採番へ変更、リアルタイム通知をWebSocketからHTTPポーリング〔`pollAfterSeconds`可変間隔〕へ変更。これに伴いmachine-router-fn/zone-consumer-fn/SQS FIFO×4/order-aggregator-fn/WebSocket一式をMVPスコープ外に変更〔§4.3, §4.4, §7.2, §7.2.1, §7.3, §8.1, §8.2, §9, §10.2, §10.5, §10.6, §11.1, §11.2, §11.3, §12, §13, §14.2, §14.6, §14.8, §14.8.1, 新設§14.12, §14.13〕）<br>
**ステータス**: 確定（実装着手可能）

---

## 目次

1. [プロジェクト概要](#1-プロジェクト概要)
2. [ビジネス要件](#2-ビジネス要件)
3. [ユーザー種別とアクセス方式](#3-ユーザー種別とアクセス方式)
4. [機能要件 — 顧客向け](#4-機能要件--顧客向け)
5. [機能要件 — スタッフ向け](#5-機能要件--スタッフ向け)
6. [注文ステータス仕様](#6-注文ステータス仕様)
7. [マシンゾーン仕様](#7-マシンゾーン仕様)
8. [ETA（製造完了予測）仕様](#8-eta製造完了予測仕様)
9. [注文状態のポーリング仕様](#9-注文状態のポーリング仕様)
10. [非機能要件](#10-非機能要件)
11. [API 設計](#11-api-設計)
12. [スコープ外（将来の布石）](#12-スコープ外将来の布石)（12.1 決済導入時のフロー）
13. [用語定義](#13-用語定義)
14. [主要な設計判断の経緯](#14-主要な設計判断の経緯)

---

## 1. プロジェクト概要

### 1.1 背景とゴール

カフェ利用者が来店前・来店と同時にスマートフォンで注文を完了し、到着と同時に商品を受け取れる体験を実現する。

- **形態**: テイクアウト専用の、アプリのように使えるWebサイト（＝PWA）
- **物理構成**: バリスタが操作する 4 台の製造マシン（ゾーン A/B/C/D）に注文を自動振り分け。完成品は自動搬送レーンで提供台へ運ばれ、既存の自動受渡システム（電子パネル＋検知カメラ）がお客様への受け渡しを検知する（§7.4）
- **価値提案**: 注文待ち行列の可視化。そして単一のレーン作業ではなく、ドリンクごとに最適化されたレーンへの振り分けにより、従来のカフェよりも「並ばないカフェ体験」を提供

### 1.2 開発方針

- **グリーンフィールド（１から作る新規開発）**: 既存資産への依存なし。新規リポジトリ `mobile-order-app` で一から構築
- **アーキテクチャ方針**: AWS サーバーレス（Lambda / DynamoDB / API Gateway）で構築。Well-Architected に準拠した設計。
- **コスト方針**: アイドル時ほぼゼロのサーバーレス従量課金。月数百円以内を目標
- **技術スタック**: フロントエンド（Next.js + PWA）/ バックエンド（Python Lambda）/ インフラ（AWS CDK TypeScript）

---

## 2. ビジネス要件

### 2.1 稼働条件


| 項目      | 値                         |
| ------- | ------------------------- |
| 営業時間    | 7:00〜18:00                |
| ピーク     | 11:00〜13:00（昼休憩時）         |
| 最大同時接続数 | 100 人                     |
| 1日の利用者数 | 約 500 人（フェルミ推定的にここは算出したい） |


### 2.2 支払い方式

- **現在**: 店頭払い（注文完了後にレジで支払い）
- アプリ内決済は MVP スコープ外（将来 Stripe 連携できるよう設計に布石を残す）

---

## 3. ユーザー種別とアクセス方式

### 3.1 顧客


| 項目     | 仕様                              |
| ------ | ------------------------------- |
| アクセス方法 | 店内 QR コード読み取りなど、既知 URL から直接アクセス |
| 認証     | 不要（MVP では sessionId で識別）        |
| デバイス   | スマートフォン（ブラウザ / PWA）             |
| URL    | `cafe.example.com/`             |


### 3.2 スタッフ

スタッフが担当する役割は、ゾーン担当（Zone A〜D、製造）の4択。従来検討していた提供台担当というログインロールは廃止し、受け渡し工程は既存の自動受渡システム（電子パネル＋検知カメラ、§7.4）に置き換える。


| 項目       | 仕様                                            |
| -------- | --------------------------------------------- |
| アクセス方法   | `cafe.example.com/staff` にアクセス → Cognito ログイン |
| 認証       | Cognito（共有アカウント 1 つ。オーナーがコンソール画面から手動作成）       |
| 同時ログイン   | 最大 4 台のタブレットが同じアカウントで同時ログイン可。各タブレットはログインのたびに A/B/C/D のいずれかを選ぶ（§5.1） |
| デバイス     | タブレット（スマホならなんでも可能）                            |
| 個人別アカウント | スコープ外（将来の拡張）                                  |


---

## 4. 機能要件 — 顧客向け

### 4.1 メニュー閲覧

- カテゴリ別にメニューを一覧表示する
- 各商品に以下の組み合わせ（バリアント）を設定できる


| バリアント | 選択肢                          |
| ----- | ---------------------------- |
| 個数    | 1〜上限（サーバー側で検証）               |
| 温度    | ホット / アイス（商品によって制限あり）        |
| サイズ   | S / M / L（エスプレッソ非使用はS / Mのみ） |


### 4.2 カート

- 商品をカートに追加できる
- カート内の個数を変更できる
- カート内の明細を削除できる
- 同一商品でも温度・サイズの組み合わせが異なれば別明細として扱う

### 4.3 注文

- 「カート追加」または「すぐ注文」の 2 方式で注文を開始できる
- 注文確定時に以下を発行する
  - 注文番号（顧客に表示する短い連番。店舗・日付単位のカウンタで採番するため、日をまたぐとリセットされる。欠番が生じる場合がある。§14.12）
  - 注文 ID（システム内部で使う一意な管理番号、UUID）
  - 注文確定後、金額はサーバー側で再計算する（クライアント送信値を信用しない）
- 二重注文防止のため、重複送信を検出するための識別キー（`Idempotency-Key`）に対応する

### 4.4 注文状態の確認（MVP: ポーリング方式、§14.13）

- 注文完了（`STORE_ACCEPTED`）から受渡完了（`HANDED_OVER`）またはキャンセルまでの間、クライアントは製造ステータスとキュー位置を定期的に問い合わせる（ポーリング）
- ポーリング間隔は固定ではなく、**サーバーがレスポンスで次回間隔を指示する**（`pollAfterSeconds`）。混雑状況に応じて短く/長くなる
- フォアグラウンド中のみポーリングを継続し、バックグラウンド移行時は停止する。復帰時に即座に1回問い合わせてから再開する（`Page Visibility API`を利用）
- 詳細な技術仕様（間隔の決定条件、開始/停止条件、エラー時のふるまい）は§9を参照

### 4.5 注文キャンセル

- 注文に含まれる**全明細が `WAITING`**（§6.1）の間に限り、顧客自身が注文全体をキャンセルできる
- 1 明細でも `PREPARING` 以降に進んでいる場合、注文全体のキャンセルは不可（一部のゾーンで製造着手済みのため）
- キャンセル後、該当注文の全明細はマシンの製造キューから除外される

---

## 5. 機能要件 — スタッフ向け

### 5.1 ログイン・選択

タブレットは特定のゾーンに固定配置されるとは限らない。そのため、**ログインのたびに**、A / B / C / D の中から今回自分が担当する持ち場を選ぶ（固定の役割や記憶された選択はない）。提供台での受け渡しはスタッフのログイン操作を伴わない（§7.4）。

```
/staff (Cognito ログイン画面)
  ↓ 認証成功
選択画面（A ｜ B ｜ C ｜ D）← 単一のフラットな一覧。毎回選び直す
  └─ A/B/C/D を選択 → 注文キュー画面（選択ゾーンの明細のみ表示、§5.2）
```

### 5.2 注文キュー表示（ゾーン担当）

- 担当ゾーンに割り当てられた明細をカード形式で一覧表示する（AP-Z1、`architecture.md` §6.3）
- 新規明細が入るとリアルタイム（WebSocket）でカードが追加される
- 表示順: ゾーン内の受付順（`queueSeq` 昇順）

### 5.3 スワイプ操作（Framer Motion 実装、ゾーン担当）

「製造開始」「完成」の 2 操作は、ゾーン担当スタッフが**自分のゾーンに割り当てられた明細（商品 1 品目）単位**で行う（§6.1）。担当ゾーンの画面には、自分のゾーンの明細だけが表示される。


| 操作        | アクション                      | 単位 |
| --------- | -------------------------- | --- |
| 右スワイプ     | 製造開始（WAITING → PREPARING） | 明細（商品1品目） |
| （完成時）スワイプ | 製造完了（PREPARING → READY）    | 明細（商品1品目） |

完成した明細は自動搬送レーンに乗せ、提供台へ送る（§7.4）。ゾーン担当はこの時点で当該明細の操作を終える。

### 5.4 誤操作対策（B案：アンドゥバナー、ゾーン担当）

1. スワイプ操作後、画面下部に「取り消し」バナーを 3 秒間表示
2. 3 秒以内に「取り消し」をタップ → 状態変化をキャンセル（DynamoDB への書き込みは行わない）
3. 3 秒経過後 → DynamoDB に書き込み確定、WebSocket で顧客へ通知

### 5.5 受渡確認（スタッフ操作なし）

受渡確認はスタッフ向けアプリの機能ではない。提供台に設置された既存の自動受渡システム（電子パネル＋検知カメラ）が受け渡しを検知し、`HANDED_OVER` への遷移を自動で行う（§7.4）。スタッフ側で行うのは、完成した明細を提供台へ運び入れる操作（提供ボタン押下＋パネル指示位置への設置）のみで、Cognitoログインを伴う専用画面は不要（§7.4）。


---

## 6. 注文ステータス仕様

1 回の会計（注文）に複数商品が含まれ、かつ商品ごとに担当ゾーン（§7）が異なる場合がある。この場合、**商品ごとの製造進捗（明細ステータス）と、注文全体として顧客・スタッフへ提示するステータス（注文ステータス）を分けて管理する**。注文ステータスは明細ステータスから自動的に決まる導出値であり、直接書き換えることはない。（[Issue #11](https://github.com/Masaharu1223/AWS_OrderSystem/issues/11) で決定）

### 6.1 明細（FulfillmentLine）ステータス遷移図

明細は「注文に含まれる商品 1 品目」を指す（§7.2 のゾーン振り分けは明細単位で行われる）。各明細は、担当ゾーンごとに完全に独立して以下の遷移をたどる。他の明細の状態には一切影響されない。

```
WAITING ──→ PREPARING ──→ READY ──→ HANDED_OVER
   │
   └──→ CANCELLED（WAITING の間のみ可能）
```

| ステータス | 説明 | 遷移元 | 遷移先 |
| --- | --- | --- | --- |
| `WAITING` | 製造待ち（ゾーンのキューに並んでいる） | 注文確定 | PREPARING / CANCELLED |
| `PREPARING` | 製造中 | WAITING | READY |
| `READY` | 製造完了（この明細単体は完成済み） | PREPARING | HANDED_OVER |
| `HANDED_OVER` | 受渡済み（§6.3、検知カメラが持ち去りを検知して自動確定） | READY | — |
| `CANCELLED` | キャンセル済み | WAITING | — |

### 6.2 注文ステータス（導出値）

注文ステータスは、その注文に含まれる**全明細のステータスの組み合わせ**から一意に決まる。スタッフ・システムのどちらも注文ステータスへ直接書き込みは行わない。

| 注文ステータス | 決定条件（全明細に対する条件） |
| --- | --- |
| `STORE_ACCEPTED` | 全明細が `WAITING` |
| `PREPARING` | 1 明細以上が `PREPARING` または `READY`（かつ全明細 `READY` ではない） |
| `READY_PICKUP` | 全明細（`CANCELLED` を除く）が `READY` |
| `HANDED_OVER` | 全明細（`CANCELLED` を除く）が `HANDED_OVER` |
| `CANCELLED` | 全明細が `CANCELLED` |

`PENDING_PAYMENT`（決済待ち）と `PAYMENT_FAILED`（決済失敗）の 2 つは将来のアプリ内決済導入時の予約定義であり（§12.1）、明細がまだ作成されていない決済前の段階を表す。そのため上記の導出ルールの対象外とし、注文レコードへ直接書き込む値のまま維持する。

| ステータス | 説明 | 遷移元 | 遷移先 |
| --- | --- | --- | --- |
| `PENDING_PAYMENT` | 決済待ち（予約定義のみ、MVP未実装） | — | `STORE_ACCEPTED`（決済成功時に明細が作成される） |
| `PAYMENT_FAILED` | 決済失敗（予約定義のみ、MVP未実装） | `PENDING_PAYMENT` | — |

`READY_PICKUP`（受取可能）になるのは、**最も遅く完成したゾーンの明細が `READY` になった瞬間**である。顧客への「受取可能」通知（§9）は、この注文ステータスの変化を契機に送信する（明細単体の `READY` では送信しない）。

### 6.3 受け渡しは既存の自動受渡システムが検知する

明細が個別に `READY` になると、自動搬送レーンで提供台の電子パネルへ運ばれ、パネルが示す位置に設置される（§7.4）。**お客様が商品を取ると、パネル上部の検知カメラが持ち去りを検知し、当該明細を自動で `HANDED_OVER` に遷移させる。** スタッフによる受渡確認の操作は発生しない（§5.5）。注文ステータスの `HANDED_OVER`（§6.2）は、従来と同じ導出ルール（注文内の全明細〔`CANCELLED` を除く〕が `HANDED_OVER` になった時点で確定）のまま変わらない。複数ゾーンにまたがる注文でも、各明細のカメラ検知はゾーンごとに独立して行われ、全明細が検知された時点で注文全体が `HANDED_OVER` になる。

### 6.4 具体例

ラテ L（Zone A 担当）+ 紅茶 M（Zone D 担当）を 1 回の会計で注文した場合:

| 時刻 | Zone A の明細 | Zone D の明細 | 注文ステータス |
| --- | --- | --- | --- |
| 12:00 | WAITING | WAITING | STORE_ACCEPTED |
| 12:02 | PREPARING | WAITING | PREPARING |
| 12:04 | READY（ラテ完成） | PREPARING | PREPARING（紅茶待ち） |
| 12:07 | READY | READY（紅茶完成） | **READY_PICKUP**（ここで顧客へ通知） |
| 12:09 | HANDED_OVER（カメラが検知） | HANDED_OVER（カメラが検知） | HANDED_OVER |

ラテが 12:04 に完成しても、紅茶が揃う 12:07 まで顧客への「受取可能」通知は送らない。

---

## 7. マシンゾーン仕様

### 7.1 ゾーン定義


| ゾーン        | 対象商品                     | サイズ   | 物理マシン台数 |
| ---------- | ------------------------ | ----- | ------- |
| **Zone A** | エスプレッソ系                  | L のみ  | 1 台     |
| **Zone B** | エスプレッソ系                  | M のみ  | 1 台     |
| **Zone C** | エスプレッソ系                  | S のみ  | 1 台     |
| **Zone D** | エスプレッソ不使用（ティー系・フラペチーノ系等） | S / M | 1 台     |


### 7.2 振り分けロジック（MVP: order-fn内で同期確定、§14.12）

`order-fn` が注文確定処理の中で、ドリンクカテゴリ + サイズから**同期的に**ゾーンを決定し、ゾーン別の受付順連番（`queueSeq`）も同じ処理内でDynamoDBの原子的カウンタにより確定する。SQSや後続の非同期処理を経由しない。

```
エスプレッソ系 × L → Zone A
エスプレッソ系 × M → Zone B
エスプレッソ系 × S → Zone C
エスプレッソ不使用 × S/M → Zone D
```

### 7.2.1 なぜDynamoDB原子カウンタを採用するか（MVP方針、§14.12）

当初案（v1.7まで）では、受付順を厳密に保証するために SQS FIFO を採用していた。しかし [Issue #7](https://github.com/Masaharu1223/AWS_OrderSystem/issues/7) の検討で、**SQS FIFOが保証するのは「SQSに届いた順」であって「注文が発生した順」ではない**ため、ネットワーク遅延や再送により両者がズレうる（＝直列化点が2つ存在し、一致する保証がない）という問題が指摘された。

MVPでは以下の方式に変更する。

| 観点 | DynamoDB原子カウンタ方式（MVP採用） |
| --- | --- |
| **公平性（受付順保証）** | `order-fn` が注文確定と同じ処理内でゾーン別カウンタ（`ZONESEQ#<storeId>#<zone>`）を`UpdateItem(ADD)`で原子的にインクリメントする。直列化点は「DynamoDBの原子的インクリメント」ただ1つになり、SQS到着順とのズレが構造的に発生しない |
| **同期性** | `queueSeq`が`POST /orders`のレスポンス時点で確定済み。GSI2（ゾーン別製造キュー）にも注文確定と同時に反映される |
| **欠番の許容** | `queueSeq`はゾーン内の順序と待ち件数のカウントにのみ使う値であり、途中の書き込み失敗で番号が飛んでも実害はない（欠番を許容することで補償処理・整合性修復バッチが一切不要になる） |
| **将来の自動製造連携** | 自動製造マシンを導入する際は、確定済みの`queueSeq`を載せたメッセージをStreams経由でSQS等に流せば追加のみで済む（既に順序が確定しているため、旧設計より安全に拡張できる） |

**トレードオフと結論**: 旧案（SQS FIFO）で得られていた「疎結合バッファ」「FIFOの重複排除」は失うが、想定される注文頻度（ピークでも1秒あたり1件未満）ではバッファの必要性が薄く、重複排除は冪等キー（`Idempotency-Key`）とトランザクションの条件付き書き込みで代替できる。`machine-router-fn`・`zone-consumer-fn`・SQS FIFO×4は**MVPスコープ外**とし、必要になった時点（自動製造マシン導入等）で再導入を検討する（§12）。

### 7.3 キャンセル時のキュー除外

- キャンセルリクエスト受信時、DynamoDB の `status` を `CANCELLED` に更新する（条件: 全明細が `WAITING`）
- 該当明細のGSI2キー（`GSI2PK`/`GSI2SK`）を同じ更新でREMOVEし、ゾーン別製造キューから除外する
- SQSを経由しないため、メッセージ削除やコンシューマ側のスキップ処理は不要（旧設計より単純化）

### 7.4 店内動線（自動搬送レーン + 提供台）

ここまでの§7.1〜7.3はソフトウェア上の受付順管理（DynamoDB・SQS）の話であり、本節は店内の**物理的な**商品搬送動線を定義する。

各ゾーン（A〜D）で完成した商品は、スタッフの手渡しではなく**自動搬送レーン**（ベルトコンベア等の物理搬送設備）に乗せられ、店内中央の**提供台**まで自動で運ばれる。提供台には**既存の自動受渡システム**（電子パネル＋検知カメラ）を導入し、受け渡し工程そのものを自動化する。ゾーン担当（または搬入した者）が「提供ボタン」を押して電子パネルが指示する位置へ商品を設置すると、以降はスタッフの操作なしにパネル上部のカメラがお客様の持ち去りを検知し、受け渡しが完了する。専任の提供台担当ロールは置かない（§3.2・§5.1・§5.5）。

```mermaid
flowchart LR
    subgraph zones["ゾーン（製造機 + スタッフ用iPad）"]
        direction TB
        A["Zone A<br/>📱 iPad"]
        B["Zone B<br/>📱 iPad"]
        C["Zone C<br/>📱 iPad"]
        D["Zone D<br/>📱 iPad"]
    end
    Lane[自動搬送レーン<br/>(完成品を提供台まで搬送)]
    Panel["提供台<br/>電子パネル + 検知カメラ<br/>（既存の自動受渡システム）"]
    Customer["お客様<br/>商品を受け取る"]

    A -- 完成した商品（明細）--> Lane
    B -- 完成した商品（明細） --> Lane
    C -- 完成した商品（明細） --> Lane
    D -- 完成した商品（明細） --> Lane
    Lane -- 提供ボタン押下＋指示位置へ設置 --> Panel
    Panel -- 持ち去りをカメラが検知 --> Customer
```

**ソフトウェア上の状態遷移との対応**:

| 物理的な動き | ソフトウェア上の対応 |
| --- | --- |
| ゾーン担当が「完成」スワイプ → 商品を自動搬送レーンに乗せる | 明細ステータス `PREPARING → READY`（§5.3, §6.1） |
| 自動搬送レーンで提供台へ搬送中 | （搬送そのものはソフトウェアの管理対象外。搬送時間はETA計算に含めない、§8） |
| スタッフが「提供ボタン」を押し、電子パネル指示位置へ設置 | 既存の自動受渡システム側の操作。当システムの明細ステータスは変更しない |
| 全明細が提供台に揃う | 注文ステータスが導出で `READY_PICKUP` になる（§6.2）。顧客へ通知（§9） |
| お客様が商品を取る → カメラが持ち去りを検知 | 明細ステータス `READY → HANDED_OVER`（自動、§6.3）。全明細が揃うと注文ステータスも `HANDED_OVER` |

**留意点（MVPでの扱い）**:

- 自動搬送レーン・電子パネル・検知カメラはいずれも物理／外部システムであり、AWS側のソフトウェア費用（§10.5）とは別に導入・運用コストが発生する。本書のコスト試算（月額¥0〜数百円）はソフトウェア部分のみを対象とし、これらの費用は含まない
- 電子パネル・検知カメラは既存の自動受渡システム（外部システム）であり、当システムとの連携（提供ボタン操作時の明細登録、持ち去り検知時に当システムへ通知する方式など）の詳細は `architecture.md` 側で別途設計する
- 搬送レーンや検知カメラの故障・誤検知など物理的な異常系は、当システムでは検知できない。現場のゾーン担当が気づいた場合に声を掛け合う運用、または既存システム側の監視機能に依存する
- 複数ゾーンにまたがる注文は、電子パネル側で同一注文の商品をまとめて表示する運用を想定する。一部の商品だけ先に持ち去られた場合の扱いは既存システムの仕様に依存し、本書のスコープ外とする

---

## 8. ETA（製造完了予測）仕様

### 8.1 計測ポイント


| イベント       | タイミング                              |
| ---------- | ---------------------------------- |
| 製造開始時刻     | スタッフが「製造開始」スワイプ → `PREPARING` 遷移時刻 |
| 製造完了時刻     | スタッフが「完成」スワイプ → 明細の `READY` 遷移時刻       |
| エスプレッソ抽出時間 | 固定 25 秒（定数として扱い、学習対象から除外）          |


### 8.2 フェーズ 1（MVP）: ゾーン単位の移動平均

- 製造時間の実績（`readyAt - preparedAt`）をゾーン単位で蓄積する
- 累積合計（`sumSeconds`）と件数（`sampleCount`）を`store-fn`が明細`READY`更新時にインラインで原子的に加算し、平均値（`sumSeconds / sampleCount`）を予測値とする（旧案の指数移動平均〔EMA〕は read-modify-write が競合しうるため、原子的な`ADD`のみで完結するこの方式に変更。§14.12）
- `GET /orders/{orderId}`（ポーリング、§9）のレスポンスに `estimatedWaitMinutes` として返す

### 8.3 フェーズ 2（将来・データ 300〜500 件蓄積後）: Amazon Forecast

- ゾーン単位の細分化 → 商品単位の予測へ移行
- Amazon Forecast による時系列予測モデルの導入

---

## 9. 注文状態のポーリング仕様

MVPでは WebSocket によるリアルタイム push を採用せず、クライアントが HTTP ポーリングで状態を取得する方式を採用する（採用理由・トレードオフは§14.13）。

### 9.1 ポーリング対象と間隔

- **顧客**: `GET /orders/{orderId}`（§11.1）をポーリングする。この1エンドポイントに注文ステータス・明細ごとのステータス・キュー位置・待ち時間見込みを全て含める（`GET /orders/{orderId}/queue-position` は別途残すが、WebSocket移行後の「位置だけ」用途のための契約として維持するもので、MVPのポーリング対象としては前者に一本化する）
- **スタッフ**: `GET /stores/{storeId}/zones/{zone}/lines`（§11.1）を同様にポーリングする
- ポーリング間隔は**固定値ではなく、レスポンスに含まれる`pollAfterSeconds`フィールドでサーバーが指示する**。キューが混雑していない・完成が近い等の状況に応じて、サーバー側が間隔を短く/長く調整する

### 9.2 開始・停止・バックグラウンドの扱い

| タイミング | 動作 |
| --- | --- |
| `POST /orders` 完了直後 | 即座に1回目のポーリングを実行する |
| フォアグラウンド中 | サーバーが指示する `pollAfterSeconds` の間隔で継続する |
| バックグラウンド移行時 | `Page Visibility API`（`visibilitychange` イベント）で検知し、ポーリングを完全に停止する（バックグラウンドのタブでの通信は無駄なコストになるため） |
| バックグラウンドからの復帰 | 即座に1回問い合わせてから、通常のポーリングを再開する |
| 注文が `HANDED_OVER` / `CANCELLED` になった | ポーリングを停止する（サーバーは `pollAfterSeconds: null` を返す） |
| サーバーが429（レート制限）を返した | 指数バックオフ（5秒→10秒→20秒、上限60秒）で再試行する |
| エラーが連続する | 一定回数（5回）失敗した場合はポーリングを停止し、利用者に再読み込みを促す |
| 絶対上限 | 開始から60分が経過したら自動停止する（タブを開いたまま放置された場合の対策） |

### 9.3 なぜサーバー主導の可変間隔にするか

固定間隔（旧案: 30秒ごと）ではなく、サーバーが次回間隔を指示する方式にすることで以下の利点がある。

- 間隔のチューニングにクライアントの再デプロイが不要になる
- 負荷上昇時にサーバー側で一斉に間隔を伸ばして自衛できる
- 将来 WebSocket へ移行する際、大きな値を返すだけでポーリングを自動的に静める移行パスになり、クライアント改修が最小限で済む（§14.13）

---

## 10. 非機能要件

### 10.1 可用性

- AWS マネージドサービス（Lambda / DynamoDB / API Gateway）の AZ 冗長を活用
- SLA 目標: 99.9%（単一リージョン・単一 AZ 障害で継続稼働）

### 10.2 パフォーマンス

- API レスポンスタイム（P95）: 500ms 以内（Lambda コールドスタートを除く）
- 状態反映の遅延: ポーリング間隔（`pollAfterSeconds`、§9.1）に依存する。混雑時で最大15秒程度（WebSocket採用時の「1秒以内」からMVPでは後退するが、§14.13の判断による意図的なトレードオフ）

### 10.3 スケーラビリティ

- Lambda + DynamoDB オンデマンドにより、100 同時接続・昼ピーク時も自動スケール

### 10.4 セキュリティ


| 要件           | 対応                                        |
| ------------ | ----------------------------------------- |
| 通信経路の暗号化     | CloudFront / API Gateway で HTTPS 強制       |
| 保存データの暗号化    | DynamoDB デフォルト暗号化 / S3 SSE-S3             |
| 最小権限 IAM     | Lambda ごとに必要な DynamoDB 操作のみ許可（CDK grants） |
| 入力バリデーション    | サーバー側で enum 検証・個数上限・価格再計算                 |
| 二重注文防止       | `POST /orders` は `Idempotency-Key` に対応    |
| スロットリング      | API Gateway でバースト / レート上限を設定              |
| Staff API 認証 | Cognito JWT オーソライザー必須                     |


### 10.5 コスト

- アイドル時（夜間・週末）ほぼ ¥0
- 通常稼働時の目標: 月額 ¥1,000 未満


| サービス                 | 月額概算                     |
| -------------------- | ------------------------ |
| Lambda               | ¥0（無料枠 100 万 req/月 内）    |
| API Gateway HTTP API | ¥0〜数百円（$1.0 / 100 万 req） |
| DynamoDB オンデマンド      | 数百円                      |
| S3 + CloudFront      | 数十〜数百円                   |
| CloudWatch Logs      | 数十〜百円程度（ポーリング方式は WebSocket 方式よりリクエスト数＝ログ量が多いため、ログレベル・保持期間の設定が重要。下記参照） |
| **合計**               | **¥0〜数百円（$5 未満）**        |

**ポーリング方式のコスト注意点（§14.13）**: 顧客・スタッフ双方のポーリングにより、WebSocket方式より API Gateway / Lambda / DynamoDB へのリクエスト数が増える（試算: 500注文/日規模で月額+¥500程度）。最も見落とされやすいのは CloudWatch Logs のコストで、`status-fn` / `store-fn` など読み取り系Lambdaは `POWERTOOLS_LOG_LEVEL=WARNING` とし、ロググループの保持期間を本番14日・dev7日程度に制限する。

### 10.6 可観測性

- 全 Lambda から CloudWatch Logs へ構造化 JSON ログを出力
- CloudWatch メトリクス / アラーム: 4xx / 5xx 率（特に429の頻度）、Lambda エラー率、DynamoDB スロットル
- ポーリングのリクエスト数（req/s）、GSI2のキュー深度を監視

---

## 11. API 設計

### 11.1 エンドポイント一覧

#### Customer API（認証なし）


| メソッド   | パス                                 | Lambda    | 説明                       |
| ------ | ---------------------------------- | --------- | ------------------------ |
| GET    | `/menu`                            | menu-fn   | カテゴリ別メニュー一覧              |
| GET    | `/menu/{productId}`                | menu-fn   | 商品詳細                     |
| GET    | `/cart/{sessionId}`                | cart-fn   | カート取得                    |
| POST   | `/cart/{sessionId}/items`          | cart-fn   | カートに商品を追加                |
| PUT    | `/cart/{sessionId}/items/{itemId}` | cart-fn   | カート明細の個数変更               |
| DELETE | `/cart/{sessionId}/items/{itemId}` | cart-fn   | カート明細の削除                 |
| POST   | `/orders`                          | order-fn  | 注文確定（Idempotency-Key 対応） |
| GET    | `/orders/{orderId}`                | status-fn | 注文ステータスのポーリング            |
| GET    | `/orders/{orderId}/queue-position` | status-fn | キュー位置と待ち時間の取得            |
| PATCH  | `/orders/{orderId}/cancel`         | order-fn  | 注文キャンセル（STORE_ACCEPTED のみ）     |


#### Staff API（Cognito 認証必須）


| メソッド  | パス                         | Lambda   | 説明                                     |
| ----- | -------------------------- | -------- | -------------------------------------- |
| GET   | `/stores/{storeId}/zones/{zone}/lines` | store-fn | ゾーン別明細一覧（ポーリング対象、§9.1） |
| PATCH | `/orders/{orderId}/lines/{lineId}/status` | store-fn | 明細ステータス更新（WAITING→PREPARING / PREPARING→READY のみ許可） |
| PATCH | `/orders/{orderId}/lines/{lineId}/handover` | store-fn | 自動受渡システムからの持ち去り検知通知を受け、明細を`READY→HANDED_OVER`に更新（Cognitoではなく別認証、`architecture.md` §7.5参照） |


> `HANDED_OVER` は既存の自動受渡システムからの検知により自動確定するため、Staff APIでの手動更新の対象外（§7.4）。

#### WebSocket API（将来。MVPでは未実装、§14.13）


| ルート           | Lambda        | 説明                                               |
| ------------- | ------------- | ------------------------------------------------ |
| `$connect`    | connect-fn    | connectionId を ConnectionTable に保存               |
| `$disconnect` | disconnect-fn | ConnectionTable から connectionId を削除              |
| `$default`    | push-fn       | クライアント → サーバー送信の受け口（当面スタブ。push は EventBridge 駆動） |


> **補足（インターフェース上の注記）**
>
> - `GET /menu/{productId}`: menu-fn は全メニュー（小規模・キャッシュ可能）を取得し、その中から該当商品を返す。パスに category を含めなくてよい。
> - **queueSeqの採番**: `order-fn` が注文確定処理の中で、ゾーン別カウンタ（`ZONESEQ#<storeId>#<zone>`）を原子的にインクリメントして`queueSeq`を同期確定する（§7.2、§14.12）。SQSやEventBridgeを経由する非同期処理は存在しない。
> - **push-fn / machine-router-fn / zone-consumer-fn / order-aggregator-fn**: いずれもMVPスコープ外（§12）。将来WebSocketやSQSベースの非同期パイプラインを再導入する際に検討する。

### 11.2 Lambda 関数一覧

**MVP（5関数）**

| 関数名                 | 属する API         | 役割                                                                                 |
| ------------------- | --------------- | ---------------------------------------------------------------------------------- |
| `menu-fn`           | Customer        | メニュー取得                                                                             |
| `cart-fn`           | Customer        | カート操作                                                                              |
| `order-fn`          | Customer        | 注文確定（ゾーン確定・`queueSeq`同期採番・全明細を一括作成）・キャンセル（§7.2, §14.12）                                                               |
| `status-fn`         | Customer        | 注文状態・キュー位置のポーリング応答（明細単位で算出、§9）                                                          |
| `store-fn`          | Staff           | ゾーン別明細一覧・明細ステータス更新（ポーリング応答）・ZONESTAT更新                                                          |

**将来（MVPスコープ外、§12・§14.12・§14.13）**

| 関数名                 | 属する API         | 役割                                                                                 |
| ------------------- | --------------- | ---------------------------------------------------------------------------------- |
| `machine-router-fn` | 内部（EventBridge） | 明細ごとにゾーン A〜D への SQS 振り分け（自動製造マシン連携が現実化した時点で再検討）                                                           |
| `zone-consumer-fn`  | 内部（SQS FIFO）    | 4 ゾーンの SQS FIFO を単一 Lambda で消費し、明細ごとの受付順（`queueSeq`）を確定            |
| `order-aggregator-fn` | 内部（DynamoDB Streams直接） | 明細から注文の導出ステータスを再計算し、GSI1・イベントを更新（GSI1の消費者〔売上集計・注文一覧画面〕が必要になった時点で再検討）                              |
| `connect-fn`        | WebSocket       | 接続時の connectionId 保存                                                               |
| `disconnect-fn`     | WebSocket       | 切断時の connectionId 削除                                                               |
| `push-fn`           | 内部（EventBridge） | 顧客・スタッフへの WebSocket push（ポーリングのコストや体感速度が問題になった時点で再検討）                                                           |
| `payment-fn`        | Staff（将来）       | 決済 Webhook 受信（雛形のみ）                                                                |


### 11.3 DynamoDB テーブル設計（概要）

**テーブル名**: `MobileOrderTable`（論理名。CDK が生成する物理名を環境変数で Lambda に注入）

注文は「注文ヘッダ（META）」と「明細（LINE、商品1品目ごと）」に分けて持つ（§6の明細単位モデルに対応）。詳細なキー設計・具体例は `architecture.md` §6 を正とする。

主要アクセスパターン（概要）:


| #   | パターン            | キー                                                      |
| --- | --------------- | ------------------------------------------------------- |
| AP1 | カテゴリ別メニュー一覧     | PK=`MENU`（固定）, SK prefix=`PROD#`                       |
| AP2 | 商品詳細            | PK=`MENU`（固定）, SK=`PROD#<productId>`                   |
| AP3 | カート取得           | PK=`CART#<sessionId>`, SK prefix=`ITEM#`                |
| AP4 | カート明細 CRUD      | PK=`CART#<sessionId>`, SK=`ITEM#<productId>#<variant>`  |
| AP5 | 注文ヘッダ＋全明細取得     | PK=`ORDER#<orderId>`（SK 全件 Query）                       |
| AP6 | 注文ステータス（導出）ポーリング | PK=`ORDER#<orderId>`（全LINE + META を読み、§6.2 のルールで導出）     |
| AP7 | 店舗：導出ステータス別注文一覧（**MVPでは能動的な読み手が無い**、下記注記） | GSI1PK=`STORE#<storeId>#<derivedStatus>`, GSI1SK=`<createdAt>` |
| AP8 | 明細ステータス更新       | PK=`ORDER#<orderId>`, SK=`LINE#<lineId>`                |
| AP-Z1 | 店舗：ゾーン別明細製造キュー一覧 | GSI2PK=`ZONE#<storeId>#<zone>`, GSI2SK=`<queueSeq>`   |
| AP-Z2 | キュー位置（前方件数カウント） | GSI2PK=`ZONE#<storeId>#<zone>`, `queueSeq < :myseq` の COUNT |
| AP-Z3 | ゾーン別受付順の採番（`order-fn`が同期実行、§7.2） | PK=`ZONESEQ#<storeId>#<zone>`（原子的インクリメント）              |
| AP-Z4 | ゾーン別製造時間統計の参照/更新（移動平均、§8.2） | PK=`ZONESTAT#<storeId>#<zone>`                          |
| AP-Z5 | 注文番号の日次採番（新規、§4.3） | PK=`ORDERNUM#<storeId>#<yyyy-mm-dd>`（原子的インクリメント、TTL 7日） |

> AP1/AP2 は商品点数が少ない前提のため、`category`ごとにパーティションを分けず**全商品をPK=`MENU`固定の単一パーティションに集約**する。AP1（`GET /menu`）は`Query PK=MENU`で全件取得後、in-memoryで`category`属性によりグループ化して返す。AP2（`GET /menu/{productId}`）は`GetItem PK=MENU, SK=PROD#<productId>`で単体取得する（O(1)、`category`をパスに含めなくてよい。§11.1の補足参照）。追加のGSIは不要。
>
> **欠番許容の原則（§14.12）**: AP-Z3・AP-Z5の採番は、後続の書き込みが失敗しても採番済みの番号を再利用・補填しない（欠番を許容する）。`queueSeq`は順序と件数カウント（AP-Z2）にのみ使用し、注文番号も連続性を保証しない値として扱うため、欠番があっても機能に影響しない。この前提により、採番失敗時の補償処理・整合性修復バッチが不要になる。
>
> **AP7（GSI1）がMVPで能動的な読み手を持たない理由**: 自動受渡システム導入（§5.5, §6.3, §7.4）により、スタッフが「受渡待ち注文一覧」を見て手動確認する運用自体が無くなった。MVPでは`order-fn`が作成時・キャンセル時にのみGSI1PKを書き込み、`PREPARING`/`READY_PICKUP`/`HANDED_OVER`への追随更新は行わない（この追随は将来`order-aggregator-fn`を再導入する際に対応する、§12・§14.12）。テーブル定義・GSI自体は将来の売上集計・注文一覧画面のために維持する。

GSI は **GSI1（注文の導出ステータス別一覧）+ GSI2（ゾーン別製造キュー、WAITING/PREPARINGのみ保持するスパースインデックス）の2本**。

**ConnectionTable**（WebSocket 用）:

- PK=`connectionId`, SK=`orderId`, TTL=2 時間

---

## 12. スコープ外（将来の布石）

以下は MVP では実装しないが、設計に布石（フィールド予約・コメント等）を残す。


| 機能                  | 状態         | 布石の内容                                                                                                                                  |
| ------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| アプリ内決済（Stripe）      | 将来         | `PENDING_PAYMENT` / `PAYMENT_FAILED` ステータス定義。`payment-fn` の雛形Lambda、CDK コメント。実装時はStripe Webhookの`event.id`をDynamoDBに保存し、再送による二重更新を防止する |
| Amazon Forecast ETA | 将来（データ蓄積後） | フェーズ 1 の EMA 実装を Forecast に差し替え可能な設計                                                                                                   |
| Web Push 通知         | 将来         | バックグラウンドでの通知。Service Worker + FCM                                                                                                      |
| 個人別スタッフアカウント        | 将来         | Cognito ユーザープールは流用可能                                                                                                                   |
| スタッフ側の注文キャンセル（強制クローズ） | 将来         | 作り間違い・No-show・いたずら注文をスタッフが手動でクローズする機能。MVP の `store-fn` は前進遷移のみ扱う（§5.3）ため対象外とする。将来追加時は許可する遷移元ステータスの範囲、キャンセル理由の記録要否、顧客への通知要否を検討する（[Issue #9](https://github.com/Masaharu1223/AWS_OrderSystem/issues/9)） |
| WebSocketによるリアルタイムpush一式 | 将来（§14.13） | `connect-fn`/`disconnect-fn`/`push-fn`、`ConnectionTable`。ポーリングのコストや体感速度が問題になった時点で導入する。REST側の実装（status-fn等）はほぼそのまま流用できる設計とする |
| SQS FIFOによる製造キュー中核 | 将来（§14.12） | `machine-router-fn`、`zone-consumer-fn`、SQS FIFO×4。自動製造マシン連携が現実化した時点で再検討する。DynamoDB側で既に`queueSeq`が確定しているため、追加のみで導入できる |
| `order-aggregator-fn` + EventBridgeファンアウト | 将来（§14.12） | GSI1（導出ステータス別注文一覧）の消費者（売上集計・注文一覧画面等）が必要になった時点で導入する |
| 受取時刻指定              | スコープ外      | —                                                                                                                                      |
| 店内飲食対応              | スコープ外      | —                                                                                                                                      |
| ネイティブアプリ            | スコープ外      | —                                                                                                                                      |


### 12.1 アプリ内決済 導入時のフロー（将来実装時の詳細）

MVP では「商品選択（メニュー・カート）→ 注文 → `STORE_ACCEPTED`（注文受付＝製造キューへの振り分け対象）」という、決済ステップを持たないフローで実装する。商用化にあたってアプリ内決済を導入する際は、`STORE_ACCEPTED` の手前に決済ステップを挟み込む形で、以下の手順を想定する。

1. `POST /orders` → 注文を `PENDING_PAYMENT`（決済待ち）状態で作成し、`orderId` を返す
2. クライアントを決済プロバイダ（Stripe 等）のチェックアウト画面へリダイレクトする
3. 決済完了 → Stripe Webhook を受信 → `payment-fn` が注文を `STORE_ACCEPTED` に更新し、WebSocket で push する
4. クライアントがチェックアウトから復帰 → WebSocket で状態変化を受信 → キュー位置の表示を開始する

- 決済失敗時は `PAYMENT_FAILED` に遷移し、カート内容は保持する（再決済できるようにするため）
- 「商品選択」自体は決済の有無に関わらず共通のフローであり、決済ステップだけを注文確定と製造キュー投入の間に後付けで挟み込める設計とする（§14.1 の Cognito 格上げと同様、`order-fn` 自体の構造は変えずに済む）

---

## 13. 用語定義


| 用語                      | 定義                                                      |
| ----------------------- | ------------------------------------------------------- |
| **ゾーン**                 | 物理マシン 1 台に対応した製造エリア（A / B / C / D）                      |
| **SQS FIFO**            | 各ゾーン専用の注文キュー。ゾーンごとに 1 本（計 4 本）。**MVPでは未使用（将来、§12）**       |
| **queueSeq**            | ゾーン内での明細の受付順を表す連番。`order-fn`がDynamoDB原子カウンタで同期採番する（欠番を許容、§14.12） |
| **pollAfterSeconds**    | ポーリングAPIのレスポンスに含まれる、次回問い合わせまでの待機秒数（サーバー主導の可変間隔、§9） |
| **ETA**                 | 製造完了予測時刻（Estimated Time of Arrival）                     |
| **sessionId**           | 未ログイン顧客を識別する UUID（ブラウザにローカル保持）                          |
| **orderId**             | 注文を一意に識別するシステム内部の UUID                                  |
| **注文番号**                | 顧客に提示する短い連番（例: #042）。店舗・日付単位のカウンタで採番し、欠番を許容する（§4.3, §14.12） |
| **variant**             | 商品の温度（hot/iced）+ サイズ（S/M/L）の組み合わせ                       |
| **ConnectionTable**     | WebSocket の connectionId と orderId を対応付ける DynamoDB テーブル。**MVPでは未使用（将来、§12）** |
| **Page Visibility API** | ブラウザが提供するタブのフォアグラウンド/バックグラウンド状態を検知する API                |
| **アンドゥバナー**             | スタッフのスワイプ操作後 3 秒間表示する取り消しバナー。3 秒経過で確定                   |
| **自動搬送レーン**             | 各ゾーンで完成した商品を提供台まで自動搬送する物理設備（§7.4）。ソフトウェアの管理対象外           |
| **提供台**                  | 自動搬送レーンの終点。既存の自動受渡システム（電子パネル＋検知カメラ）でお客様への受け渡しを検知する場所（§7.4） |
| **自動受渡システム**             | 提供台に設置された既存の外部システム（電子パネル＋検知カメラ）。「提供ボタン」押下で商品の設置位置を指示し、お客様の持ち去りをカメラで検知して `HANDED_OVER` を自動確定する（§5.5, §6.3, §7.4）。専任スタッフのログインロールは持たない |


---

## 14. 主要な設計判断の経緯

本セクションは確定仕様の「なぜ」を記録し、将来の再検討時に判断基準を参照できるようにするためのもの。旧プランファイル（`nifty-wishing-origami.md` / `todo-snug-scott.md`）に記録されていた議論の要点をここに統合する。

### 14.1 Cognito 認証の位置づけ変更

- 初期設計ではスコープ外（将来の「特別メニュー」機能向けの布石）だった
- スタッフ用画面（ゾーン選択・注文管理）へのアクセス制御が別途必要と判明し、**MVP必須に格上げ**した
- 個人別アカウントではなく共有アカウント1つ（最大4台タブレット同時ログイン）とし、運用をシンプルに保った

### 14.2 リアルタイム通知に WebSocket を採用（ポーリングではなく）※後に §14.13 で MVP はポーリングへ変更

- Well-Architected の「パフォーマンス効率」の観点。ポーリングよりリクエスト数が減り、状態変化時のみ通信するため無駄な DynamoDB Read が発生しない
- バックグラウンドでの WebSocket 維持はブラウザ仕様上不可能なため、`Page Visibility API` による復帰時の再接続方式を採用
- Web Push（FCM経由）は通知許可のUXコストがあるため、まずはフォアグラウンド前提の設計とし将来拡張に回した

> **本節の結論は判断履歴として残す**（削除しない）。[Issue #8](https://github.com/Masaharu1223/AWS_OrderSystem/issues/8) の検討により、MVPでは本節の結論を覆し**ポーリング方式を採用**した。理由と、コストで劣る選択をした根拠は§14.13を参照。

### 14.3 注文キャンセル方式の比較と選定


| 方式                                   | 速度  | コスト          | 複雑さ | 判定                                 |
| ------------------------------------ | --- | ------------ | --- | ---------------------------------- |
| A. DynamoDB 条件付き Update + 消費側の冪等チェック | 高   | 低（追加コストほぼゼロ） | 低   | **採用**                             |
| B. SQS メッセージ削除                       | 中   | 中            | 中   | 見送り（`ReceiptHandle` の保存コストが発生するため） |
| C. DynamoDB Streams でキャンセルイベントを伝播    | 低   | 低            | 高   | 見送り（複雑さが見合わない）                     |


### 14.4 マシンルーティング: ルールベース先行、Bedrock は段階導入

- MVP は if / switch の**ルールベース**で十分と判断（実装・テストが容易）
- Bedrock は「キュー深度に応じた動的振り分け」「処理時間の異常検知」など複雑な最適化が必要になった段階（フェーズ2）で初めて価値が出るため、データが溜まってから導入判断する

### 14.5 ETA 予測: EMA 先行、Amazon Forecast は段階導入（Bedrock は不採用）

- ETA 予測は時系列予測タスクであるため、汎用 LLM の Bedrock ではなく専用の時系列予測サービスである **Amazon Forecast** を採用先として選定。
- データが 300〜500 件蓄積されるまでは、フェーズ1としてゾーン単位（A, B, C, Dの４つ）の指数移動平均（EMA）で代替する段階的導入方針とした。

### 14.6 EventBridge と SQS の使い分け方針（MVPでは両方とも未使用、§14.12・§14.13）

- **SQS FIFO**: 「送り主1つ→受け取り先1つへ、順番を守って渡したいだけ」の処理に使う。ゾーンごとの製造キュー（1台のマシンへ注文を受付順に渡す）がこれに当たる
- **EventBridge**: 「1つの出来事（例: 注文が入った、ステータスが変わった）を、複数の異なる担当処理に同時に知らせたい」場合に使う。「注文が入った」という1つの出来事をきっかけに、マシンへの振り分け・顧客への通知・（将来の）売上集計・（将来の）ポイント付与など、複数の処理を同時に走らせられる
- この使い分けの考え方自体は将来の拡張時にも有効だが、**MVPでは§14.12・§14.13の決定によりSQS FIFO・EventBridgeのどちらも使用しない**。ゾーン確定と`queueSeq`採番は`order-fn`内の同期処理に、状態通知はポーリングに置き換えた
- 将来、自動製造マシン連携や売上集計等でこれらのサービスが必要になった場合、`order-fn`自体のコードを変更せず追加できる設計は維持している（§7.2.1）

### 14.7 将来のイベント駆動拡張ポイント（MVPでは未導入）

以下は現時点で導入する具体的な要件がないため見送っているが、該当する要件が来た時点で `order-fn` 等の既存コードを触らず後付けできるよう、境界だけ意識しておく。


| サービス                      | 何を解決するか                                           | 導入するとしたらどんな場面か                             |
| ------------------------- | ------------------------------------------------- | ------------------------------------------ |
| **EventBridge Scheduler** | 時刻ベースの自動処理                                        | 朝の本日メニュー自動有効化、18時の受付自動停止、放置注文の自動キャンセル      |
| **Step Functions**        | 複数ステップの処理を順序・リトライ・失敗時の巻き戻し（補償）・待機付きで確実に実行するワークフロー | アプリ内決済（在庫引当→決済→確定、失敗時は返金・在庫戻し）、受取タイムアウトの管理 |


### 14.8 実装ロードマップ（統合版）

基本機能（Phase 0〜6）と追加要件（Phase A〜F）を実装順の目安として統合する。実際の着手順は状況に応じて調整してよい。

**基本機能**


| Phase | 内容                             |
| ----- | ------------------------------ |
| 0     | 設計確定（本ドキュメント）                  |
| 1     | CDK基盤: DynamoDBテーブル、ヘルスチェックAPI |
| 2     | メニューAPI + フロントのメニュー表示          |
| 3     | カートAPI + カートUI（個数/温度/サイズ）      |
| 4     | 注文API + 注文確認 + 顧客ポーリング         |
| 5     | 店舗向け注文管理（ステータス更新・ポーリング）        |
| 6     | PWA化・CloudFront配信・仕上げ          |


**追加要件（基本機能の上に積む）**

Phase C・Aは §14.12・§14.13 の決定（`queueSeq`同期採番・ポーリング方式の採用）に伴い**将来（MVPスコープ外）へ変更**した。Phase B（キュー位置API）はorder-fn同期採番の完成後にすぐ実施可能になった。


| Phase | 内容                                                               |
| ----- | ---------------------------------------------------------------- |
| B     | キュー位置API仕上げ（`GET /orders/{orderId}` へのポーリング契約統合、§9・§14.12）        |
| D     | キャンセルAPI（`PATCH /orders/{orderId}/cancel`）                       |
| A（将来） | WebSocket API + connect/disconnect/push Lambda + ConnectionTable（§14.13） |
| C（将来） | machine-router-fn + zone-consumer-fn + SQS FIFO ×4（§14.12）         |
| E（将来） | payment-fn + `PENDING_PAYMENT` フロー                               |
| F（将来） | Bedrock動的ルーティング／Amazon Forecast                                  |


#### 14.8.1 実装方式に垂直スライスを採用

本判断は機能要件ではなく、§14.8のロードマップを実行可能な単位へ分解するための実装方針である。当初の実装ロードマップは、CDK基盤、各API、フロントエンドをPhase単位で順に構築する目安として整理していた。一方、実装計画を具体化する過程で、技術レイヤーごとにまとめて実装すると、API Gateway・Lambda・DynamoDB・IAM・環境変数・フロントエンド間の接続不備やAPI仕様のずれが後工程まで発見されないリスクがあると判断した。特に本システムは複数のAWSマネージドサービスを接続するサーバーレス構成であり、個々のコードが正しくてもサービス間の配線が誤っていれば機能しない。

検討した実装方式は以下の通り。

| 方式 | 内容 | 評価 |
| --- | --- | --- |
| 技術レイヤー単位 | CDK基盤、全バックエンド、全フロントエンドを層ごとにまとめて実装する | 統合作業が後半に集中し、配線不備や仕様のずれの発見が遅れるため見送り |
| ロードマップのPhase順をそのまま実施 | §14.8のPhase 0〜6、A〜Fを記載順に実装する | 全体像の把握には適するが、後続機能が必要とするデータや非同期処理の依存順を十分に表せないため、着手順としては固定しない |
| **機能単位の垂直スライス** | 1機能についてインフラ配線、バックエンド、APIの入出力仕様、テスト、フロントエンド、dev環境での動作確認までを一貫して完成させる | **採用**。小さな単位でエンド・ツー・エンドの動作を確認でき、統合リスクを早期に解消できる |

1スライスは、原則として次の流れを完結させる。

1. CDK配線と、仕様どおりの固定レスポンスを返すスタブを実装する
2. `cdk synth`とdev環境へのデプロイで、API Gateway・Lambda・IAM・環境変数等の配線を確認する
3. ドメインロジック、データアクセス、入力検証、エラー処理と単体・結合テストを実装する
4. フロントエンドを実際のAPI仕様へ接続する
5. 再度dev環境へデプロイし、利用者視点の機能として完了基準を満たすことを確認する

最初のスライスを`menu-fn`とし、共通Python Lambdaコンストラクト、コード配置、エラーエンベロープ、API Gateway統合、assertion/synthテストという開発ループの雛形を確立する。後続のcart/order/status等はこの雛形を再利用し、機能ごとの差分へ集中する。

実装順は§14.8のロードマップを基礎としつつ、依存関係に応じて調整する。

**（2026-07-31改訂）** 当初は「店舗向けゾーン別一覧はGSI2の`queueSeq`を必要とするため、`store-fn`より先にmachine-router/zone-consumer/order-aggregatorを含む非同期製造キューを成立させる」という順序としていた。しかし§14.12の決定（`order-fn`が`queueSeq`を同期採番する）により、GSI2は`order-fn`完了時点で即座に埋まるようになったため、**この依存関係自体が解消された**。結果として非同期製造キュー（旧Phase C）を先行させる理由が消滅し、`store-fn`は`order-fn`の直後に着手できる。新しい着手順は`tasks/todo.md`§1に記録する。


### 14.9 CDKスタック構成（初期設計。§7.2以降の拡張に伴い要再設計）

モノレポ（`infra/` + `web/`）構成を前提に、以下3スタックへの分割を出発点とする。ただし WebSocket API・SQS FIFO・EventBridge・Cognito 追加後のスタック粒度（`stateless-stack` に含めるか、リアルタイム系を別スタックに分けるか）は未確定であり、architecture.md清書時に再設計が必要。


| スタック              | 内容                                                    |
| ----------------- | ----------------------------------------------------- |
| `stateful-stack`  | DynamoDBテーブル（GSI1+GSI2、PITR有効）、`removalPolicy=RETAIN`、終了保護 |
| `stateless-stack` | Lambda、API Gateway、IAM(grants)、CloudWatch             |
| `frontend-stack`  | S3（非公開）+ CloudFront（OAC）                              |


- `dev` / `prod` は CDK Stage で分離し、環境ごとに独立したリソースをデプロイする

### 14.10 複数ゾーン注文のステータス確定方式（明細単位モデルの採用）

- 当初の設計（v1.1）では、注文ステータス（`STORE_ACCEPTED`/`PREPARING`/`READY_PICKUP` 等）を注文レコード 1 件に対して 1 つだけ持たせていた。しかし §7.2 の通りゾーン振り分けは商品（明細）ごとに行われるため、1 注文が複数ゾーンにまたがると、後から処理したゾーンの状態で先の状態が上書きされ、一部のゾーンにしか商品が表示されない・一部完成のみで受取可能と誤通知される、といった問題が発生することが判明した（[Issue #6](https://github.com/Masaharu1223/AWS_OrderSystem/issues/6) で指摘、後に [Issue #11](https://github.com/Masaharu1223/AWS_OrderSystem/issues/11) の決定に伴い Issue #6 は削除）
- 対応として、商品（明細）ごとに独立したステータスを持つモデルへ変更した（§6）。注文全体のステータスは、全明細の状態から導出する値とし、直接の書き込み対象から外した
- 受取可能（`READY_PICKUP`）は「最も遅いゾーンの明細が完成した瞬間」に確定する。全ゾーンが完了するまで顧客への完成通知を待つことで、駆け付けた顧客が一部の商品しか受け取れない事態を防ぐ
- 受け渡しは、渡し忘れ事故を防ぐため商品単位ではなく注文単位で一括確定する（§6.3）
- キャンセル可否の境界は「全明細が製造未着手（`WAITING`）かどうか」に変更した（§4.5）。従来の「注文ステータスが `STORE_ACCEPTED` かどうか」は、新モデルでは全明細 `WAITING` の場合の導出値と一致するため、実質的な挙動は変わらない
- 本節に付随する技術的な検討事項（明細ごとのデータモデル詳細、同一商品複数個の明細分割方針、ゾーン別一覧のアクセスパターン再設計等）は、architecture.md 改訂時に確定する（[Issue #11](https://github.com/Masaharu1223/AWS_OrderSystem/issues/11) の検討事項を参照）

### 14.11 提供台の受渡確認を自動検知方式へ変更（提供台担当ロールの廃止）

- 当初案（§7.4 初版）では、提供台に「提供台担当」という専任のログインロールを置き、スタッフが自動搬送レーンで届いた商品と注文内容を目視照合したうえで、注文単位の「受渡確認」ボタンを手動で押す設計だった
- 見直しの結果、既存の自動受渡システム（電子パネル＋検知カメラ）を採用する方式に変更した。スタッフ（搬入した者）が「提供ボタン」を押して電子パネル指示位置へ商品を設置し、以降はお客様の持ち去りをカメラが検知して `HANDED_OVER` を自動確定する（§5.5, §6.3, §7.4）
- この変更に伴い、スタッフ選択画面（§5.1）は A/B/C/D の4択に戻り、提供台担当というログインロール・専用画面は廃止した（§3.2）
- `READY_PICKUP` への導出ルール（§6.2、全明細 `READY` で確定）は変更していない。変わったのは `READY → HANDED_OVER` の遷移トリガーのみで、「スタッフの手動一括確認」から「検知カメラによる自動検知（明細ごとに独立、全明細検知で注文単位のHANDED_OVERへ導出）」に置き換わった
- 電子パネル・検知カメラとの連携方式（提供ボタン操作時のデータ登録、持ち去り検知時の当システムへの通知方法など）は既存の外部システムとの統合であり、詳細は `architecture.md` 側で別途設計する

### 14.12 ゾーン別受付順の採番方式をSQS FIFOからDynamoDB原子カウンタへ変更

- [Issue #7](https://github.com/Masaharu1223/AWS_OrderSystem/issues/7) の検討により、旧設計（§7.2.1旧版、SQS FIFO採用）が抱えていた根本的な問題が判明した: **SQS FIFOが保証するのは「SQSに届いた順」であって「注文が発生した順」ではない**。受付順の直列化点が「DynamoDBへの書き込み順」と「SQSへの到着順」の2つに分かれており、ネットワーク遅延や再送によって両者がズレうる。同一ゾーン内で後から注文した客が先に受け取れてしまう不公平が起こり得る、という実害のある問題だった
- 対応として、`order-fn`が注文確定処理の中でゾーン別カウンタ（`ZONESEQ#<storeId>#<zone>`）を`UpdateItem(ADD)`により**同期的に原子インクリメント**し、`queueSeq`を確定する方式に変更した（§7.2, §7.2.1）。直列化点を「DynamoDBの原子的インクリメント」ただ1つに集約することで、ズレる相手が存在しない設計にした
- この変更に伴い、途中の書き込み失敗時に採番済みの番号が使われない「欠番」が生じうるが、`queueSeq`は順序と待ち件数のカウント（AP-Z2）にのみ使う値であるため実害がないと判断し、**欠番を許容する**方針とした。これにより、失敗時の補償処理・整合性修復バッチが一切不要になる
- 副次効果として、GSI2（ゾーン別製造キュー）が`order-fn`完了と同時に埋まるようになり、非同期パイプライン（旧`machine-router-fn`/`zone-consumer-fn`/SQS FIFO×4）を経由する必要がなくなった。これらはMVPスコープ外とし、自動製造マシン連携が現実化した時点で再導入を検討する（§12, §14.8.1）
- 冪等キー（`Idempotency-Key`）衝突時に他人の注文内容を返さないよう、`IDEMPOTENCY`アイテムに`sessionId`を保存し照合する仕様を追加した（セキュリティ上の考慮事項）

### 14.13 リアルタイム通知をWebSocketからHTTPポーリングへ変更（MVP）

- [Issue #8](https://github.com/Masaharu1223/AWS_OrderSystem/issues/8) の検討により、旧設計（§14.2）が抱えていた2つの問題が判明した
  1. DynamoDB StreamsのMODIFYイベントは、業務的に意味のある変化（例: ステータスの変化）があったかを区別できない。`queueSeq`のみが更新された場合など、ステータスが変わっていないのに「変更通知」が誤発火する懸念があった
  2. 新規注文がスタッフ画面へリアルタイム通知される経路が現行設計に存在しなかった。複数ゾーンにまたがる注文では「ゾーン単位の通知」が必要になり、設計がさらに複雑化する
- 対応として、MVPでは**HTTPポーリング**方式を採用した（§9）。ポーリング間隔はサーバーが`pollAfterSeconds`で指示する可変方式とし、固定間隔のポーリングより無駄な問い合わせを削減する
- **この決定はコストで劣る選択を意図的に行っている**: 試算では定常運用コストはWebSocket方式の方が安い（月額差にして数十倍）。それでもポーリングを採用した理由は、`ConnectionTable`・切断済みコネクションの掃除・`push-fn`・EventBridgeルール・再接続ロジックという運用部品をMVPから丸ごと外し、まず業務ロジック（キュー管理・ステータス導出）自体の正しさを検証することを優先したためである。将来の再検討時に「なぜ安い方を選ばなかったか」を忘れないよう、この理由を明記しておく
- REST側の実装（`status-fn`のレスポンス契約等）はWebSocket移行後もほぼそのまま流用できる設計とした（§9.3）。要件§9.1相当のバックグラウンド復帰時REST再取得は、WebSocket採用時でも必須要件であり続けるため、今回作る実装が無駄になることはない
